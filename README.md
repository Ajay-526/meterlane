# Meterlane

```
meterlane/     Go API               :8080
web-static/    Next.js public site  :3001
admin/         Next.js admin        :3002
```

```bash
git clone https://github.com/Ajay-526/meterlane.git
cd meterlane
```

Public site photos live in `web-static/public/` (`hero.jpg`, `still.jpg`, `palace.jpg`, `sprigs.jpg`, `portrait-a.jpg`, `portrait-b.jpg`, `logo.png`). If git pull does not include those binaries, copy them from the project folder into `web-static/public/`.
